name: release

on:
  workflow_dispatch:
    inputs:
      tag:
        required: true
        description: Tag to trigger release
        type: string
      prerelease:
        required: true
        description: Whether to release as a prerelease
        type: boolean
  workflow_call:
    inputs:
      tag:
        type: string
        required: true
      prerelease:
        type: boolean
        required: true

permissions:
  contents: read

jobs:
  release:
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@1b05615854632b887b69ae1be8cbefe72d3ae423 # v2.6.0
        with:
          egress-policy: audit

      - name: Checkout Repo
        uses: actions/checkout@8ade135a41bc03ea155e62e844d188df1ea18608 # v3.6.0
        with:
          ref: refs/tags/${{ inputs.tag }}

      - name: Create release
        uses: ncipollo/release-action@6c75be85e571768fa31b40abf38de58ba0397db5 # v1.13.0
        with:
          tag: ${{ inputs.tag }}
          skipIfReleaseExists: true
          generateReleaseNotes: true
          draft: ${{ inputs.prerelease }}
          prerelease: ${{ inputs.prerelease }}

      - name: Generate SBOM
        uses: anchore/sbom-action@41f7a6c033dbcdf78917f23b652c8b8146298c85 # v0.15.4
        with:
          artifact-name: sbom-${{ github.event.repository.name }}-${{ inputs.tag }}.spdx.json
          output-file: sbom-${{ github.event.repository.name }}-${{ inputs.tag }}.spdx.json
          upload-artifact-retention: 1

      - name: Publish SBOM
        uses: anchore/sbom-action/publish-sbom@41f7a6c033dbcdf78917f23b652c8b8146298c85 # v0.15.4
        with:
          sbom-artifact-match: ".*${{ inputs.tag }}.spdx\\.json"
